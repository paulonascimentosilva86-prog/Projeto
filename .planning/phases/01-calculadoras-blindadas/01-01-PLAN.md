---
phase: 01-calculadoras-blindadas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/calculadoras/decimal-utils.ts
autonomous: true
requirements:
  - CALC-06
must_haves:
  truths:
    - "npx vitest run passa sem erros de configuracao"
    - "import Decimal from 'decimal.js' funciona nos modulos de calculo"
    - "a funcao arredondar() aplica ROUND_HALF_UP e retorna string com 2 casas decimais"
    - "operacoes com R$ 0.01 nao produzem erro de ponto flutuante"
  artifacts:
    - path: "package.json"
      provides: "dependencias vitest e decimal.js"
      contains: "vitest"
    - path: "vitest.config.ts"
      provides: "configuracao do test runner"
      contains: "defineConfig"
    - path: "src/calculadoras/decimal-utils.ts"
      provides: "utilitario decimal com ROUND_HALF_UP"
      exports: ["arredondar", "toDecimal", "porcentagem"]
  key_links:
    - from: "src/calculadoras/decimal-utils.ts"
      to: "decimal.js"
      via: "import Decimal from 'decimal.js'"
      pattern: "import Decimal"
    - from: "vitest.config.ts"
      to: "tests/calculadoras/"
      via: "include glob"
      pattern: "tests/calculadoras"
---

<objective>
Configurar infraestrutura de testes (Vitest + decimal.js) e criar utilitario de precisao financeira que todas as calculadoras usarao.

Purpose: Sem esta base, nenhuma calculadora pode ser testada deterministicamente. O decimal-utils elimina o problema de ponto flutuante em todos os calculos monetarios.
Output: package.json com scripts de teste, vitest.config.ts, src/calculadoras/decimal-utils.ts exportando arredondar/toDecimal/porcentagem.
</objective>

<execution_context>
@/home/user/Projeto/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Projeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/Projeto/.planning/ROADMAP.md
@/home/user/Projeto/.planning/REQUIREMENTS.md
@/home/user/Projeto/.planning/phases/01-calculadoras-blindadas/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inicializar package.json e vitest.config.ts</name>
  <read_first>
    /home/user/Projeto/claude-code-juridico/remotion-videos/package.json (referencia de formato npm deste projeto)
  </read_first>
  <files>package.json, vitest.config.ts</files>
  <behavior>
    - package.json tem scripts: "test" = "vitest run", "test:watch" = "vitest", "test:coverage" = "vitest run --coverage"
    - package.json tem devDependencies: vitest ^3.x, @vitest/coverage-v8 ^3.x
    - package.json tem dependencies: decimal.js ^10.x
    - vitest.config.ts importa defineConfig de vitest
    - vitest.config.ts inclui glob "tests/calculadoras/**/*.test.ts"
    - vitest.config.ts define globals: true para evitar imports de describe/it/expect
    - npx vitest run --passWithNoTests retorna exit 0 (validacao pos-criacao)
  </behavior>
  <action>
    1. Criar package.json na raiz /home/user/Projeto com:
       - name: "claude-code-juridico-testes"
       - version: "1.0.0"
       - type: "module"
       - scripts: { "test": "vitest run", "test:watch": "vitest", "test:coverage": "vitest run --coverage" }
       - dependencies: { "decimal.js": "^10.4.3" }
       - devDependencies: { "vitest": "^3.0.0", "@vitest/coverage-v8": "^3.0.0" }

    2. Criar vitest.config.ts na raiz /home/user/Projeto com:
       ```typescript
       import { defineConfig } from 'vitest/config'
       export default defineConfig({
         test: {
           globals: true,
           include: ['tests/calculadoras/**/*.test.ts'],
           environment: 'node',
         },
       })
       ```

    3. Instalar dependencias:
       cd /home/user/Projeto && npm install
  </action>
  <verify>
    <automated>cd /home/user/Projeto && npx vitest run --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>npm install completo sem erros, npx vitest run --passWithNoTests retorna exit 0, node_modules/decimal.js existe</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar src/calculadoras/decimal-utils.ts e teste de validacao</name>
  <read_first>
    /home/user/Projeto/.planning/phases/01-calculadoras-blindadas/01-RESEARCH.md (secao "Stack" e "Validation Architecture")
  </read_first>
  <files>src/calculadoras/decimal-utils.ts, tests/calculadoras/decimal-utils.test.ts</files>
  <behavior>
    - toDecimal(valor: number | string): Decimal — converte para Decimal sem perda
    - arredondar(valor: Decimal | number | string, casas?: number): string — aplica ROUND_HALF_UP, padrao 2 casas, retorna string "1234.56"
    - porcentagem(base: Decimal | number | string, pct: Decimal | number | string): Decimal — calcula base * pct / 100 com ROUND_HALF_UP
    - multiplicar(a, b): Decimal — produto exato sem ponto flutuante
    - somar(...valores): Decimal — soma N valores Decimal

    Casos de teste obrigatorios (RED primeiro, depois GREEN):
    - arredondar(0.1 + 0.2) === "0.30" (trap classico de ponto flutuante)
    - arredondar(1000 * 0.02) === "20.00" (multa 2%)
    - arredondar(1000 * 0.01) === "10.00" (juros 1%)
    - porcentagem(1000, 30).toString() produz "300" (honorarios 30%)
    - arredondar(0.005) === "0.01" (ROUND_HALF_UP arredonda 0.5 para cima)
    - arredondar(0.004) === "0.00" (trunca abaixo de 0.005)
    - toDecimal("1234.56").equals(new Decimal("1234.56")) === true
  </behavior>
  <action>
    RED: Criar tests/calculadoras/decimal-utils.test.ts com todos os casos acima usando describe/it/expect.
    Rodar: cd /home/user/Projeto && npx vitest run tests/calculadoras/decimal-utils.test.ts — deve FALHAR (modulo nao existe).

    GREEN: Criar src/calculadoras/decimal-utils.ts:
    ```typescript
    import Decimal from 'decimal.js'

    Decimal.set({ rounding: Decimal.ROUND_HALF_UP })

    export function toDecimal(valor: number | string | Decimal): Decimal {
      return new Decimal(valor)
    }

    export function arredondar(valor: number | string | Decimal, casas = 2): string {
      return new Decimal(valor).toDecimalPlaces(casas, Decimal.ROUND_HALF_UP).toFixed(casas)
    }

    export function porcentagem(base: number | string | Decimal, pct: number | string | Decimal): Decimal {
      return new Decimal(base).mul(new Decimal(pct)).div(100)
    }

    export function multiplicar(a: number | string | Decimal, b: number | string | Decimal): Decimal {
      return new Decimal(a).mul(new Decimal(b))
    }

    export function somar(...valores: Array<number | string | Decimal>): Decimal {
      return valores.reduce((acc, v) => new Decimal(acc).add(new Decimal(v)), new Decimal(0)) as Decimal
    }
    ```

    Rodar: cd /home/user/Projeto && npx vitest run tests/calculadoras/decimal-utils.test.ts — deve PASSAR.
  </action>
  <verify>
    <automated>cd /home/user/Projeto && npx vitest run tests/calculadoras/decimal-utils.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Todos os 7 casos de teste passam (0 failed)
    - arredondar(0.1 + 0.2) retorna "0.30" (nao "0.30000000000000004")
    - arredondar(0.005) retorna "0.01" (ROUND_HALF_UP confirmado)
    - src/calculadoras/decimal-utils.ts exporta: toDecimal, arredondar, porcentagem, multiplicar, somar
    - grep "ROUND_HALF_UP" src/calculadoras/decimal-utils.ts retorna resultado
  </acceptance_criteria>
  <done>Todos os testes do decimal-utils passam, ROUND_HALF_UP confirmado por teste, modulo exporta todas as funcoes necessarias</done>
</task>

</tasks>

<verification>
cd /home/user/Projeto && npx vitest run tests/calculadoras/ 2>&1
Esperado: todos os testes passam, nenhum erro de configuracao, nenhum erro de importacao decimal.js
</verification>

<success_criteria>
- npx vitest run --passWithNoTests sai com exit 0
- npx vitest run tests/calculadoras/decimal-utils.test.ts passa 100% (7/7 testes)
- node_modules/decimal.js/ existe em /home/user/Projeto/
- src/calculadoras/decimal-utils.ts usa Decimal.ROUND_HALF_UP (grep confirma)
- Nenhuma operacao aritmetica nativa (*, /, +, -) em decimal-utils.ts exceto dentro de new Decimal()
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-calculadoras-blindadas/01-01-SUMMARY.md` com:
- Stack instalada (vitest versao, decimal.js versao)
- Funcoes exportadas por decimal-utils.ts
- Resultado dos testes (N passed)
- Caminho dos arquivos criados
</output>
