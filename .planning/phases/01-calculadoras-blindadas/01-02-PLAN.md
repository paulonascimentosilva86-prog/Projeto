---
phase: 01-calculadoras-blindadas
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/calculadoras/condominial.ts
  - tests/calculadoras/condominial.test.ts
autonomous: true
requirements:
  - CALC-01

must_haves:
  truths:
    - "Multa de 2% sobre R$ 1.000,00 retorna exatamente R$ 20,00"
    - "Juros de 1%/mes sobre R$ 1.020,00 por 3 meses retorna R$ 30,60"
    - "Honorarios de 30% sobre subtotal sao calculados sem ponto flutuante"
    - "npx vitest run tests/calculadoras/condominial.test.ts passa 100%"
  artifacts:
    - path: "src/calculadoras/condominial.ts"
      provides: "Funcao calcularCondominial com todas as parcelas"
      exports: ["calcularCondominial", "CondominialInput", "CondominialResult"]
    - path: "tests/calculadoras/condominial.test.ts"
      provides: "Suite de testes com valores deterministicos"
      contains: "describe('calcularCondominial'"
  key_links:
    - from: "src/calculadoras/condominial.ts"
      to: "src/calculadoras/decimal-utils.ts"
      via: "import { arredondar, porcentagem, somar, toDecimal }"
      pattern: "from.*decimal-utils"
---

<objective>
Implementar calcularCondominial() com TDD: multa 2%, juros 1%/mes, correcao INPC, honorarios 30% — todos usando decimal.js via decimal-utils.

Purpose: A calculadora condominial e usada para cobrar devedores de condominio. Um erro de centavo pode invalidar a execucao extrajudicial ou gerar contestacao.
Output: src/calculadoras/condominial.ts com funcao pura testavel + tests/calculadoras/condominial.test.ts com suite completa.
</objective>

<execution_context>
@/home/user/Projeto/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Projeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/Projeto/.planning/ROADMAP.md
@/home/user/Projeto/.planning/REQUIREMENTS.md
@/home/user/Projeto/.planning/phases/01-calculadoras-blindadas/01-RESEARCH.md
@/home/user/Projeto/.planning/phases/01-calculadoras-blindadas/01-01-SUMMARY.md
</context>

<interfaces>
<!-- De src/calculadoras/decimal-utils.ts (criado em 01-01) -->
```typescript
import Decimal from 'decimal.js'
Decimal.set({ rounding: Decimal.ROUND_HALF_UP })

export function toDecimal(valor: number | string | Decimal): Decimal
export function arredondar(valor: number | string | Decimal, casas?: number): string
export function porcentagem(base: number | string | Decimal, pct: number | string | Decimal): Decimal
export function multiplicar(a: number | string | Decimal, b: number | string | Decimal): Decimal
export function somar(...valores: Array<number | string | Decimal>): Decimal
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Criar testes para calcularCondominial</name>
  <read_first>
    /home/user/Projeto/.planning/phases/01-calculadoras-blindadas/01-RESEARCH.md (secao "Calculadora Condominial", formula chain e test cases)
    /home/user/Projeto/claude-code-juridico/.claude/commands/calculadora-condominial.md (para entender o command original)
  </read_first>
  <files>tests/calculadoras/condominial.test.ts</files>
  <behavior>
    Formula chain (do RESEARCH.md):
    - Multa = Principal × 2%
    - Juros = (Principal + Multa) × 1% × meses_atraso
    - Correcao = Principal × fatorINPC (fator acumulado, ex: 1.05 = 5% de INPC)
    - Subtotal = Principal + Multa + Juros + Correcao
    - Honorarios = Subtotal × 30%
    - Total = Subtotal + Honorarios

    Input type:
    ```typescript
    interface CondominialInput {
      principal: number       // valor da divida em R$
      mesesAtraso: number     // quantos meses em atraso
      fatorINPC: number       // ex: 1.05 para 5% de correcao INPC acumulada
    }
    ```

    Output type:
    ```typescript
    interface CondominialResult {
      principal: string       // "1000.00"
      multa: string           // "20.00"
      juros: string           // "30.60"
      correcao: string        // "50.00"
      subtotal: string        // "1100.60"
      honorarios: string      // "330.18"
      total: string           // "1430.78"
    }
    ```

    Casos de teste obrigatorios (usar valores com resultado exato verificavel):
    1. caso minimo: principal=1000, mesesAtraso=1, fatorINPC=1 (sem correcao)
       - multa = "20.00", juros = "10.20", correcao = "0.00", subtotal = "1030.20", honorarios = "309.06", total = "1339.26"
    2. caso tipico: principal=1000, mesesAtraso=6, fatorINPC=1.05
       - multa = "20.00", juros = "61.20", correcao = "50.00", subtotal = "1131.20", honorarios = "339.36", total = "1470.56"
    3. caso com centavos: principal=1333.33, mesesAtraso=1, fatorINPC=1
       - Verificar que multa = arredondar(1333.33 * 0.02) sem ponto flutuante
    4. caso extreme: principal=50000, mesesAtraso=60, fatorINPC=1.4
       - Verificar que total e um numero grande mas preciso

    IMPORTANTE: Calcular os valores esperados manualmente com decimal.js antes de escrever os expects.
    Para o caso 1: juros = (1000 + 20) * 0.01 * 1 = 10.20 ✓
    Para o caso 2: juros = (1000 + 20) * 0.01 * 6 = 61.20 ✓, correcao = 1000 * (1.05 - 1) = 50.00 ✓
  </behavior>
  <action>
    Criar tests/calculadoras/condominial.test.ts com os 4 casos de teste acima.
    O arquivo deve importar: import { calcularCondominial } from '../../src/calculadoras/condominial'
    Rodar: cd /home/user/Projeto && npx vitest run tests/calculadoras/condominial.test.ts
    Confirmar que falha com "Cannot find module" (RED confirmado).
  </action>
  <verify>
    <automated>cd /home/user/Projeto && npx vitest run tests/calculadoras/condominial.test.ts 2>&1 | grep -E "FAIL|Cannot find module|Error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo tests/calculadoras/condominial.test.ts existe
    - Contem pelo menos 4 describe/it blocks com valores numericos fixos
    - Rodar o teste retorna falha por modulo ausente (nao por erro de sintaxe)
    - grep "calcularCondominial" tests/calculadoras/condominial.test.ts retorna resultado
  </acceptance_criteria>
  <done>Arquivo de teste existe com 4+ casos deterministicos, falha corretamente por modulo ausente</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implementar calcularCondominial</name>
  <read_first>
    /home/user/Projeto/src/calculadoras/decimal-utils.ts (funcoes disponiveis: toDecimal, arredondar, porcentagem, somar, multiplicar)
    /home/user/Projeto/tests/calculadoras/condominial.test.ts (casos de teste que precisam passar)
  </read_first>
  <files>src/calculadoras/condominial.ts</files>
  <behavior>
    - Importa APENAS de decimal-utils (nao usa operacoes aritmeticas nativas exceto dentro de toDecimal)
    - Correcao = Principal × (fatorINPC - 1) — se fatorINPC=1.05, correcao = principal * 0.05
    - Todos os campos do resultado sao strings com 2 casas decimais via arredondar()
    - Funcao e pura (sem side effects, sem IO)
    - Exporta CondominialInput, CondominialResult (interfaces), calcularCondominial (funcao)
  </behavior>
  <action>
    Criar src/calculadoras/condominial.ts implementando a formula chain do RESEARCH.md.
    Usar apenas funcoes de decimal-utils para aritmetica.
    Rodar: cd /home/user/Projeto && npx vitest run tests/calculadoras/condominial.test.ts
    Todos os testes devem PASSAR (GREEN).
    Se algum caso falhar, recalcular o valor esperado no teste ou corrigir a formula — nao aproximar.
  </action>
  <verify>
    <automated>cd /home/user/Projeto && npx vitest run tests/calculadoras/condominial.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run tests/calculadoras/condominial.test.ts: 0 failed
    - grep "ROUND_HALF_UP\|decimal-utils" src/calculadoras/condominial.ts retorna resultado (usa decimal-utils)
    - grep -E "\*|/|\+" src/calculadoras/condominial.ts | grep -v "decimal-utils\|import\|//" retorna vazio (sem aritmetica nativa)
    - Caso 1: multa="20.00" e juros="10.20" confirmados no output do teste
    - Caso 2: correcao="50.00" e total="1470.56" confirmados no output do teste
  </acceptance_criteria>
  <done>Todos os 4+ testes passam, implementacao usa decimal-utils exclusivamente, sem aritmetica nativa</done>
</task>

</tasks>

<verification>
cd /home/user/Projeto && npx vitest run tests/calculadoras/condominial.test.ts 2>&1
Esperado: X passed | 0 failed — com valores de multa, juros, correcao, honorarios e total verificados
</verification>

<success_criteria>
- npx vitest run tests/calculadoras/condominial.test.ts: 100% passed
- src/calculadoras/condominial.ts exporta calcularCondominial, CondominialInput, CondominialResult
- Multa 2% sobre R$ 1.000 = R$ 20,00 (confirmado por teste)
- Juros 1%/mes verificado em pelo menos 2 meses diferentes
- Honorarios 30% calculados sobre subtotal (nao sobre principal)
- Nenhum numero magico: todos os percentuais como constantes nomeadas ou argumentos
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-calculadoras-blindadas/01-02-SUMMARY.md` com:
- Funcoes exportadas por condominial.ts
- Casos de teste cobertos (N passed)
- Valores verificados (ex: multa 2% = R$ 20,00 confirmado)
- Formula chain implementada
</output>
