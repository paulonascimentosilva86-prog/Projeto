---
phase: 01-calculadoras-blindadas
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/calculadoras/distrato.ts
  - tests/calculadoras/distrato.test.ts
autonomous: true
requirements:
  - CALC-02

must_haves:
  truths:
    - "Retencao de 25% sobre R$ 200.000 retorna exatamente R$ 50.000,00 (regime comum)"
    - "Retencao de 50% sobre R$ 200.000 retorna exatamente R$ 100.000,00 (patrimonio afetacao)"
    - "Taxa de fruicao de 0.5%/mes e deduzida corretamente do valor a devolver"
    - "npx vitest run tests/calculadoras/distrato.test.ts passa 100%"
  artifacts:
    - path: "src/calculadoras/distrato.ts"
      provides: "Funcao calcularDistrato cobrindo 3 cenarios Lei 13.786/2018"
      exports: ["calcularDistrato", "DistratoInput", "DistratoResult"]
    - path: "tests/calculadoras/distrato.test.ts"
      provides: "Suite com 3 cenarios x 2 regimes = 6+ casos"
      contains: "describe('calcularDistrato'"
  key_links:
    - from: "src/calculadoras/distrato.ts"
      to: "src/calculadoras/decimal-utils.ts"
      via: "import { arredondar, porcentagem, somar, toDecimal }"
      pattern: "from.*decimal-utils"
---

<objective>
Implementar calcularDistrato() com TDD cobrindo os 3 cenarios da Lei 13.786/2018: desistencia do comprador (regime comum e patrimonio afetacao) e atraso da construtora.

Purpose: O distrato imobiliario e uma das areas de maior litigio em Sao Paulo. A retencao errada de 25% vs 50% pode custar dezenas de milhares de reais ao cliente.
Output: src/calculadoras/distrato.ts com logica de 3 cenarios + tests/calculadoras/distrato.test.ts com 6+ casos deterministicos.
</objective>

<execution_context>
@/home/user/Projeto/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Projeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/Projeto/.planning/ROADMAP.md
@/home/user/Projeto/.planning/REQUIREMENTS.md
@/home/user/Projeto/.planning/phases/01-calculadoras-blindadas/01-RESEARCH.md
@/home/user/Projeto/.planning/phases/01-calculadoras-blindadas/01-01-SUMMARY.md
</context>

<interfaces>
<!-- De src/calculadoras/decimal-utils.ts (criado em 01-01) -->
```typescript
export function toDecimal(valor: number | string | Decimal): Decimal
export function arredondar(valor: number | string | Decimal, casas?: number): string
export function porcentagem(base: number | string | Decimal, pct: number | string | Decimal): Decimal
export function multiplicar(a: number | string | Decimal, b: number | string | Decimal): Decimal
export function somar(...valores: Array<number | string | Decimal>): Decimal
```

<!-- Regras Lei 13.786/2018 (do RESEARCH.md) -->
// Desistencia comprador - regime comum: retencao maxima 25% do pago (art. 67-A, §5)
// Desistencia comprador - patrimonio afetacao: retencao maxima 50% do pago (art. 67-A, §2)
// Deducoes: comissao corretagem + IPTU + condominio + taxa fruicao (0.5%/mes do valor do imovel)
// Valor a devolver = totalPago - retencao - deducoes
// Atraso construtora (>180 dias): devolucao integral + correcao INCC + multa contratual
// Arrependimento (7 dias): devolucao integral sem retencao
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Criar testes para calcularDistrato</name>
  <read_first>
    /home/user/Projeto/.planning/phases/01-calculadoras-blindadas/01-RESEARCH.md (secao "Calculadora Distrato", todos os cenarios)
    /home/user/Projeto/claude-code-juridico/.claude/commands/calculadora-distrato.md (command original para entender os cenarios)
  </read_first>
  <files>tests/calculadoras/distrato.test.ts</files>
  <behavior>
    Input type:
    ```typescript
    type CenarioDistrato = 'desistencia_comprador' | 'atraso_construtora' | 'arrependimento'
    type RegimeDistrato = 'comum' | 'afetacao'

    interface DistratoInput {
      cenario: CenarioDistrato
      regime: RegimeDistrato
      totalPago: number           // total ja pago pelo comprador em R$
      valorImovel: number         // valor do imovel
      mesesUso: number            // meses que usou (para taxa fruicao)
      comissaoCorretagem: number  // valor da comissao ja paga em R$
      fatorINCC: number           // ex: 1.08 para 8% de correcao INCC (so atraso construtora)
      multaContratual: number     // % de multa (so atraso construtora), ex: 1 para 1%
    }
    ```

    Output type:
    ```typescript
    interface DistratoResult {
      totalPago: string
      retencao: string
      taxaFruicao: string
      comissaoCorretagem: string
      valorDevolver: string
      prazoDevoucao: string   // "180 dias" | "30 dias apos habite-se" | "60 dias" | "7 dias"
      observacoes: string[]
    }
    ```

    Casos de teste obrigatorios com valores calculados:
    1. Desistencia regime comum sem uso: totalPago=200000, valorImovel=500000, mesesUso=0, comissaoCorretagem=15000, regime=comum
       - retencao = 200000 * 0.25 = "50000.00"
       - taxaFruicao = "0.00"
       - valorDevolver = 200000 - 50000 - 15000 - 0 = "135000.00"
       - prazoDevoucao = "180 dias"

    2. Desistencia patrimonio afetacao sem uso: totalPago=200000, valorImovel=500000, mesesUso=0, comissaoCorretagem=15000, regime=afetacao
       - retencao = 200000 * 0.50 = "100000.00"
       - valorDevolver = 200000 - 100000 - 15000 = "85000.00"
       - prazoDevoucao = "30 dias apos habite-se"

    3. Desistencia regime comum com 6 meses de uso: totalPago=200000, mesesUso=6, valorImovel=500000, comissaoCorretagem=15000
       - taxaFruicao = 500000 * 0.005 * 6 = "15000.00"
       - valorDevolver = 200000 - 50000 - 15000 - 15000 = "120000.00"

    4. Atraso construtora: totalPago=200000, fatorINCC=1.08, multaContratual=1, valorImovel=500000
       - valorDevolver = 200000 * 1.08 + (200000 * 0.01) = "216000.00" + "2000.00" = "218000.00"
       - prazoDevoucao = "60 dias"

    5. Arrependimento (7 dias): totalPago=50000
       - retencao = "0.00"
       - valorDevolver = "50000.00"
       - prazoDevoucao = "7 dias"

    6. Caso com centavos: totalPago=123456.78 para testar precisao decimal
  </behavior>
  <action>
    Criar tests/calculadoras/distrato.test.ts com os 6 casos de teste.
    Importar: import { calcularDistrato } from '../../src/calculadoras/distrato'
    Rodar: cd /home/user/Projeto && npx vitest run tests/calculadoras/distrato.test.ts
    Confirmar RED (falha por modulo ausente).
  </action>
  <verify>
    <automated>cd /home/user/Projeto && npx vitest run tests/calculadoras/distrato.test.ts 2>&1 | grep -E "FAIL|Cannot find module|Error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/calculadoras/distrato.test.ts existe com 6+ casos
    - Cobre os 3 cenarios: desistencia_comprador (2x), atraso_construtora (1x), arrependimento (1x)
    - Valores esperados sao numericos fixos (nao calculos em runtime)
    - Falha com erro de modulo ausente (nao erro de sintaxe)
  </acceptance_criteria>
  <done>6+ testes com valores deterministicos, falha corretamente por modulo ausente</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implementar calcularDistrato</name>
  <read_first>
    /home/user/Projeto/src/calculadoras/decimal-utils.ts (funcoes disponíveis)
    /home/user/Projeto/tests/calculadoras/distrato.test.ts (casos que precisam passar)
  </read_first>
  <files>src/calculadoras/distrato.ts</files>
  <behavior>
    - cenario='desistencia_comprador' + regime='comum': retencao = min(25% do pago, total pago)
    - cenario='desistencia_comprador' + regime='afetacao': retencao = min(50% do pago, total pago)
    - cenario='atraso_construtora': devolucao integral com correcao INCC + multa; retencao = 0
    - cenario='arrependimento': devolucao integral sem deducoes; retencao = 0
    - taxaFruicao = valorImovel * 0.005 * mesesUso (apenas em desistencia_comprador)
    - valorDevolver nao pode ser negativo (minimo 0)
    - observacoes: array de strings com alertas legais (ex: "Art. 67-A Lei 13.786/2018")
    - Toda aritmetica via decimal-utils
  </behavior>
  <action>
    Criar src/calculadoras/distrato.ts com a funcao calcularDistrato.
    Usar switch/if por cenario e regime.
    Rodar: cd /home/user/Projeto && npx vitest run tests/calculadoras/distrato.test.ts
    Todos os testes devem PASSAR.
  </action>
  <verify>
    <automated>cd /home/user/Projeto && npx vitest run tests/calculadoras/distrato.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run tests/calculadoras/distrato.test.ts: 0 failed
    - Caso 1: retencao="50000.00", valorDevolver="135000.00"
    - Caso 2: retencao="100000.00", valorDevolver="85000.00"
    - Caso 3: taxaFruicao="15000.00", valorDevolver="120000.00"
    - Caso 5 (arrependimento): retencao="0.00"
    - grep "decimal-utils" src/calculadoras/distrato.ts retorna resultado
  </acceptance_criteria>
  <done>Todos os 6+ testes passam, 3 cenarios cobertos, aritmetica via decimal-utils</done>
</task>

</tasks>

<verification>
cd /home/user/Projeto && npx vitest run tests/calculadoras/distrato.test.ts 2>&1
Esperado: X passed | 0 failed — com retencao 25%/50% e taxa de fruicao verificados
</verification>

<success_criteria>
- npx vitest run tests/calculadoras/distrato.test.ts: 100% passed
- Retencao 25% (regime comum) sobre R$ 200.000 = R$ 50.000 confirmado por teste
- Retencao 50% (patrimonio afetacao) sobre R$ 200.000 = R$ 100.000 confirmado por teste
- Taxa de fruicao 0.5%/mes calculada corretamente com decimal.js
- Arrependimento em 7 dias retorna valorDevolver integral (0 retencao)
- prazoDevoucao correto para cada cenario
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-calculadoras-blindadas/01-03-SUMMARY.md` com:
- 3 cenarios cobertos (desistencia, atraso, arrependimento)
- Casos de teste e valores verificados
- Regras da Lei 13.786/2018 implementadas
</output>
