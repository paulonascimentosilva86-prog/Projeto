---
phase: 01-calculadoras-blindadas
plan: 04
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/calculadoras/aluguel.ts
  - tests/calculadoras/aluguel.test.ts
autonomous: true
requirements:
  - CALC-03

must_haves:
  truths:
    - "Reajuste por IGP-M 10% sobre R$ 2.000 retorna exatamente R$ 2.200,00"
    - "Reajuste acumulado de 2 anos sem reajuste e calculado via produto de fatores (nao soma)"
    - "Indice negativo (IGP-M negativo) e tratado: novo valor nunca menor que atual"
    - "npx vitest run tests/calculadoras/aluguel.test.ts passa 100%"
  artifacts:
    - path: "src/calculadoras/aluguel.ts"
      provides: "Funcao calcularReajusteAluguel para IGP-M, IPCA, INPC"
      exports: ["calcularReajusteAluguel", "AluguelInput", "AluguelResult"]
    - path: "tests/calculadoras/aluguel.test.ts"
      provides: "Suite com reajuste simples, acumulado, negativo e comparativo 3 indices"
      contains: "describe('calcularReajusteAluguel'"
  key_links:
    - from: "src/calculadoras/aluguel.ts"
      to: "src/calculadoras/decimal-utils.ts"
      via: "import { arredondar, porcentagem, toDecimal }"
      pattern: "from.*decimal-utils"
---

<objective>
Implementar calcularReajusteAluguel() com TDD cobrindo reajuste por IGP-M, IPCA e INPC — incluindo reajuste acumulado (mais de 1 ano sem reajuste) e tratamento de indice negativo.

Purpose: Um erro no reajuste de aluguel repetido por 5 anos pode acumular centenas de reais de prejuizo. O indice negativo (IGP-M) tem sido controverso e precisa de tratamento explicito.
Output: src/calculadoras/aluguel.ts + tests/calculadoras/aluguel.test.ts com 5+ casos.
</objective>

<execution_context>
@/home/user/Projeto/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Projeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/Projeto/.planning/ROADMAP.md
@/home/user/Projeto/.planning/REQUIREMENTS.md
@/home/user/Projeto/.planning/phases/01-calculadoras-blindadas/01-RESEARCH.md
@/home/user/Projeto/.planning/phases/01-calculadoras-blindadas/01-01-SUMMARY.md
</context>

<interfaces>
<!-- De src/calculadoras/decimal-utils.ts (criado em 01-01) -->
```typescript
export function toDecimal(valor: number | string | Decimal): Decimal
export function arredondar(valor: number | string | Decimal, casas?: number): string
export function porcentagem(base: number | string | Decimal, pct: number | string | Decimal): Decimal
export function multiplicar(a: number | string | Decimal, b: number | string | Decimal): Decimal
export function somar(...valores: Array<number | string | Decimal>): Decimal
```

<!-- Formula do RESEARCH.md -->
// Novo = Atual × (1 + indice/100)
// Para acumulado: Novo = Atual × produto_dos_fatores_mensais
// Indice negativo: alguns contratos proibem reducao — novoValor = max(Novo, Atual)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Criar testes para calcularReajusteAluguel</name>
  <read_first>
    /home/user/Projeto/.planning/phases/01-calculadoras-blindadas/01-RESEARCH.md (secao "Calculadora Aluguel")
    /home/user/Projeto/claude-code-juridico/.claude/commands/calculadora-aluguel.md (command original)
  </read_first>
  <files>tests/calculadoras/aluguel.test.ts</files>
  <behavior>
    Input type:
    ```typescript
    type IndiceAluguel = 'IGP-M' | 'IPCA' | 'INPC'

    interface AluguelInput {
      valorAtual: number          // R$ aluguel atual
      indice: IndiceAluguel
      percentualIndice: number    // variacao percentual do indice, ex: 10.5 para 10.5%
      permitirReducao?: boolean   // default false — se false, nunca reduz o aluguel
    }

    // Para reajuste acumulado (mais de 1 ano sem reajuste):
    interface AluguelAcumuladoInput {
      valorAtual: number
      fatoresAnuais: number[]     // ex: [5.0, 3.5, 8.2] para 3 anos de indices
      permitirReducao?: boolean
    }
    ```

    Output type:
    ```typescript
    interface AluguelResult {
      valorAtual: string
      indice: string
      percentualAplicado: string
      novoValor: string
      diferenca: string           // novo - atual
      variacaoPercentual: string  // percentual real aplicado
    }

    interface AluguelComparativoResult {
      valorAtual: string
      resultados: Record<IndiceAluguel, AluguelResult>
      maisVantajoso: IndiceAluguel  // para o locador: maior valor
    }
    ```

    Casos de teste obrigatorios:
    1. Reajuste simples IGP-M 10%: valorAtual=2000, percentualIndice=10
       - novoValor = "2200.00", diferenca = "200.00"

    2. Reajuste simples IPCA 5.79%: valorAtual=3000, percentualIndice=5.79
       - novoValor = arredondar(3000 * 1.0579) = "3173.70"

    3. Indice negativo sem permitirReducao: valorAtual=2000, percentualIndice=-3, permitirReducao=false
       - novoValor = "2000.00" (nao reduz, mantém atual)
       - variacaoPercentual = "0.00"

    4. Indice negativo com permitirReducao=true: valorAtual=2000, percentualIndice=-3
       - novoValor = "1940.00"

    5. Reajuste acumulado 2 anos: valorAtual=2000, fatoresAnuais=[10, 5]
       - Ano1 = 2000 * 1.10 = 2200
       - Ano2 = 2200 * 1.05 = 2310.00
       - novoValor = "2310.00" (produto, nao soma: nao "2300.00")

    6. Comparativo 3 indices: valorAtual=2000 com IGP-M=10%, IPCA=5.79%, INPC=4.5%
       - maisVantajoso = "IGP-M"
  </behavior>
  <action>
    Criar tests/calculadoras/aluguel.test.ts com os 6 casos de teste.
    Importar: import { calcularReajusteAluguel, calcularReajusteAcumulado } from '../../src/calculadoras/aluguel'
    Rodar: cd /home/user/Projeto && npx vitest run tests/calculadoras/aluguel.test.ts
    Confirmar RED.
  </action>
  <verify>
    <automated>cd /home/user/Projeto && npx vitest run tests/calculadoras/aluguel.test.ts 2>&1 | grep -E "FAIL|Cannot find module|Error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/calculadoras/aluguel.test.ts existe com 6+ casos
    - Caso de indice negativo presente (com e sem permitirReducao)
    - Caso de reajuste acumulado presente (produto de fatores, nao soma)
    - Falha por modulo ausente (nao sintaxe)
  </acceptance_criteria>
  <done>6+ casos deterministicos cobrindo todos os cenarios do RESEARCH.md, RED confirmado</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implementar calcularReajusteAluguel</name>
  <read_first>
    /home/user/Projeto/src/calculadoras/decimal-utils.ts
    /home/user/Projeto/tests/calculadoras/aluguel.test.ts
  </read_first>
  <files>src/calculadoras/aluguel.ts</files>
  <behavior>
    - Formula: novoValor = toDecimal(valorAtual).mul(toDecimal(1 + percentualIndice/100))
    - Se !permitirReducao && novoValor < valorAtual: novoValor = valorAtual
    - calcularReajusteAcumulado: reduce sobre fatoresAnuais aplicando calculo simples a cada fator
    - Todos os campos do resultado sao strings via arredondar()
    - Exporta calcularReajusteAluguel, calcularReajusteAcumulado, AluguelInput, AluguelResult
  </behavior>
  <action>
    Criar src/calculadoras/aluguel.ts.
    Rodar: cd /home/user/Projeto && npx vitest run tests/calculadoras/aluguel.test.ts
    Todos os testes devem PASSAR.
  </action>
  <verify>
    <automated>cd /home/user/Projeto && npx vitest run tests/calculadoras/aluguel.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run tests/calculadoras/aluguel.test.ts: 0 failed
    - Caso 1: novoValor="2200.00" (IGP-M 10% sobre R$ 2.000)
    - Caso 3: novoValor="2000.00" (indice negativo bloqueado)
    - Caso 5: novoValor="2310.00" (acumulado por produto, nao "2300.00")
    - grep "decimal-utils" src/calculadoras/aluguel.ts retorna resultado
  </acceptance_criteria>
  <done>Todos os 6+ testes passam, indice negativo tratado, reajuste acumulado correto por produto</done>
</task>

</tasks>

<verification>
cd /home/user/Projeto && npx vitest run tests/calculadoras/aluguel.test.ts 2>&1
Esperado: X passed | 0 failed
</verification>

<success_criteria>
- npx vitest run tests/calculadoras/aluguel.test.ts: 100% passed
- IGP-M 10% sobre R$ 2.000 = R$ 2.200,00 confirmado
- Reajuste acumulado usa produto de fatores (2000 * 1.10 * 1.05 = 2310, nao 2000 * 1.15 = 2300)
- Indice negativo sem permitirReducao: valor nao reduz
- calcularReajusteAcumulado exportado e testado
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-calculadoras-blindadas/01-04-SUMMARY.md` com:
- Indices cobertos (IGP-M, IPCA, INPC)
- Tratamento de indice negativo implementado
- Reajuste acumulado por produto de fatores
- Resultados dos testes (N passed)
</output>
