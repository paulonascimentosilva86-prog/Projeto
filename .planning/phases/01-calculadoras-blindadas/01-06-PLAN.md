---
phase: 01-calculadoras-blindadas
plan: 06
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/calculadoras/reclamatoria.ts
  - tests/calculadoras/reclamatoria.test.ts
autonomous: true
requirements:
  - CALC-05

must_haves:
  truths:
    - "Cenario sem justa causa inclui FGTS+40% calculado sobre saldo total do FGTS"
    - "Cenario justa causa retorna apenas saldo salario e ferias vencidas (sem multa FGTS)"
    - "Cenario pedido de demissao retorna sem multa FGTS e sem aviso previo indenizado"
    - "Aviso previo proporcional usa regra 30+3dias/ano, maximo 90 dias (Lei 12.506/2011)"
    - "npx vitest run tests/calculadoras/reclamatoria.test.ts passa 100%"
  artifacts:
    - path: "src/calculadoras/reclamatoria.ts"
      provides: "Funcao calcularReclamatoria com 3 cenarios e projecoes otimista/realista/acordo"
      exports: ["calcularReclamatoria", "ReclamatoriaInput", "ReclamatoriaResult", "CenarioReclamatoria"]
    - path: "tests/calculadoras/reclamatoria.test.ts"
      provides: "Suite com 3 cenarios x 3 projecoes = 9+ casos"
      contains: "describe('calcularReclamatoria'"
  key_links:
    - from: "src/calculadoras/reclamatoria.ts"
      to: "src/calculadoras/decimal-utils.ts"
      via: "import { arredondar, porcentagem, somar, toDecimal }"
      pattern: "from.*decimal-utils"
---

<objective>
Implementar calcularReclamatoria() com TDD cobrindo 3 cenarios (sem justa causa, justa causa, pedido de demissao) e 3 projecoes (otimista 100%, realista 70%, acordo 55%) com FGTS+40% e reflexos.

Purpose: O simulador de reclamatoria e a calculadora mais complexa e mais usada. Um erro no FGTS+40% ou nos reflexos de horas extras pode subestimar ou superestimar o pedido em milhares de reais.
Output: src/calculadoras/reclamatoria.ts + tests/calculadoras/reclamatoria.test.ts com 9+ casos deterministicos.
</objective>

<execution_context>
@/home/user/Projeto/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Projeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/Projeto/.planning/ROADMAP.md
@/home/user/Projeto/.planning/REQUIREMENTS.md
@/home/user/Projeto/.planning/phases/01-calculadoras-blindadas/01-RESEARCH.md
@/home/user/Projeto/.planning/phases/01-calculadoras-blindadas/01-01-SUMMARY.md
</context>

<interfaces>
<!-- De src/calculadoras/decimal-utils.ts (criado em 01-01) -->
```typescript
export function toDecimal(valor: number | string | Decimal): Decimal
export function arredondar(valor: number | string | Decimal, casas?: number): string
export function porcentagem(base: number | string | Decimal, pct: number | string | Decimal): Decimal
export function multiplicar(a: number | string | Decimal, b: number | string | Decimal): Decimal
export function somar(...valores: Array<number | string | Decimal>): Decimal
```

<!-- Regras do RESEARCH.md -->
// Verbas sem justa causa: saldo salario, aviso previo, 13 proporcional, ferias+1/3, FGTS+40%
// Verbas justa causa: saldo salario, ferias vencidas+1/3 APENAS
// Verbas pedido demissao: saldo salario, 13 proporcional, ferias proporcionais+1/3 (sem FGTS multa, sem aviso)
// Aviso previo: 30 + 3 dias por ano completo, maximo 90 dias (Lei 12.506/2011)
// FGTS multa: 40% sobre saldo total do FGTS depositado (nao sobre salario)
// Projecoes: otimista=100%, realista=70%, acordo=55% do total calculado
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Criar testes para calcularReclamatoria</name>
  <read_first>
    /home/user/Projeto/.planning/phases/01-calculadoras-blindadas/01-RESEARCH.md (secao "Simulador Reclamatoria", verbas e cenarios)
    /home/user/Projeto/claude-code-juridico/.claude/commands/simulador-reclamatoria.md (command original)
  </read_first>
  <files>tests/calculadoras/reclamatoria.test.ts</files>
  <behavior>
    Input type:
    ```typescript
    type CenarioReclamatoria = 'sem_justa_causa' | 'justa_causa' | 'pedido_demissao'

    interface ReclamatoriaInput {
      cenario: CenarioReclamatoria
      salarioBase: number           // salario mensal em R$
      mesesTrabalhados: number      // total de meses do contrato
      anosCompletos: number         // anos completos para aviso previo proporcional
      saldoDiasTrabalhados: number  // dias trabalhados no mes da demissao (0-31)
      saldoFGTS: number             // saldo total do FGTS depositado
      horasExtrasMensais?: number   // valor mensal de horas extras (se houver)
      mediaMensalVariavel?: number  // comissoes, gorjetas, etc.
    }
    ```

    Output type:
    ```typescript
    interface VerbaRescisoria {
      nome: string
      valor: string
      inclui: boolean   // false para verbas que nao se aplicam no cenario
    }

    interface ReclamatoriaResult {
      cenario: CenarioReclamatoria
      verbas: VerbaRescisoria[]
      subtotal: string
      honorarios: string      // 10% (media entre 5-15% art. 223-G CLT)
      totalBruto: string
      projecoes: {
        otimista: string      // 100% do totalBruto
        realista: string      // 70% do totalBruto
        acordo: string        // 55% do totalBruto
      }
    }
    ```

    Casos de teste obrigatorios com valores calculados:
    Parametros base reutilizados: salarioBase=5000, mesesTrabalhados=30, anosCompletos=2, saldoDiasTrabalhados=15, saldoFGTS=12000

    1. Sem justa causa — verbas obrigatorias:
       - saldoSalario = arredondar(5000 / 30 * 15) = "2500.00"
       - avisoPrevio = arredondar(5000 / 30 * (30 + 2*3)) = 36 dias = "6000.00"
       - decimo3Proporcional = arredondar(5000 / 12 * (meses_no_ano % 12)) — calcular para meses=6
       - feriasPropMaisUmTerco = arredondar((5000 / 12 * meses_ferias) * (4/3))
       - multaFGTS = arredondar(12000 * 0.40) = "4800.00"
       - inclui=true para todas as verbas acima

    2. Sem justa causa — projecoes sobre totalBruto:
       - Se totalBruto = "X": otimista = X, realista = arredondar(X * 0.70), acordo = arredondar(X * 0.55)

    3. Justa causa — apenas saldo salario e ferias vencidas:
       - saldoSalario presente com inclui=true
       - feriasVencidas presente com inclui=true
       - multaFGTS com inclui=false
       - avisoPrevio com inclui=false

    4. Pedido de demissao — sem multa FGTS e sem aviso previo indenizado:
       - multaFGTS com inclui=false
       - avisoPrevio com inclui=false (aviso cumprido ou desconto)
       - saldoSalario, 13proporcional, feriasPropMaisUmTerco com inclui=true

    5. Aviso previo proporcional: anosCompletos=0 → 30 dias, anosCompletos=2 → 36 dias, anosCompletos=20 → 90 dias (maximo)

    6. Aviso previo maximo: anosCompletos=25 → ainda 90 dias (nao ultrapassa)

    7. Honorarios 10% aplicados sobre totalBruto (nao sobre parcela separada)

    8. Com horas extras: horasExtrasMensais=500 — valor aparece em verbas com inclui=true

    9. Sem justa causa minimo (1 mes, salario=1412):
       - saldoSalario e calculavel com valores deterministicos
  </behavior>
  <action>
    Criar tests/calculadoras/reclamatoria.test.ts com 9 casos de teste.
    ATENCAO: Calcular manualmente os valores esperados para o caso 1 antes de escrever os expects.
    Para saldo salario: 5000 / 30 * 15 = 2500.00 ✓
    Para multa FGTS: 12000 * 0.40 = 4800.00 ✓
    Importar: import { calcularReclamatoria } from '../../src/calculadoras/reclamatoria'
    Rodar: cd /home/user/Projeto && npx vitest run tests/calculadoras/reclamatoria.test.ts
    Confirmar RED.
  </action>
  <verify>
    <automated>cd /home/user/Projeto && npx vitest run tests/calculadoras/reclamatoria.test.ts 2>&1 | grep -E "FAIL|Cannot find module|Error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/calculadoras/reclamatoria.test.ts existe com 9+ casos
    - 3 cenarios cobertos (sem_justa_causa, justa_causa, pedido_demissao)
    - Projecoes otimista/realista/acordo testadas com percentuais exatos
    - Aviso previo proporcional testado (incluindo maximo 90 dias)
    - FGTS+40% testado com valor fixo
    - RED confirmado
  </acceptance_criteria>
  <done>9+ casos com valores deterministicos, 3 cenarios e 3 projecoes cobertos, RED confirmado</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implementar calcularReclamatoria</name>
  <read_first>
    /home/user/Projeto/src/calculadoras/decimal-utils.ts
    /home/user/Projeto/tests/calculadoras/reclamatoria.test.ts
  </read_first>
  <files>src/calculadoras/reclamatoria.ts</files>
  <behavior>
    - PROJECOES = { otimista: 100, realista: 70, acordo: 55 } — constantes nomeadas
    - HONORARIOS_PCT = 10 — constante nomeada (media de 5-15% art. 223-G)
    - calcularAvisoPrevio(anosCompletos): 30 + (anosCompletos * 3), maximo 90
    - calcularSaldoSalario(salarioBase, diasTrabalhados): salarioBase / 30 * diasTrabalhados
    - Para cada cenario: construir array VerbaRescisoria com inclui=true/false conforme regra
    - subtotal = somar apenas verbas com inclui=true
    - honorarios = porcentagem(subtotal, 10)
    - totalBruto = subtotal + honorarios
    - projecoes = { otimista: totalBruto, realista: totalBruto*0.70, acordo: totalBruto*0.55 }
    - Toda aritmetica via decimal-utils
  </behavior>
  <action>
    Criar src/calculadoras/reclamatoria.ts com a funcao calcularReclamatoria.
    Organizar por cenario: sem_justa_causa inclui todas as verbas, justa_causa e pedido_demissao desabilitam as nao aplicaveis.
    Rodar: cd /home/user/Projeto && npx vitest run tests/calculadoras/reclamatoria.test.ts
    Todos os testes devem PASSAR.
    Se algum valor diferir por arredondamento, verificar a formula — nao ajustar o expect para "colar" no resultado.
  </action>
  <verify>
    <automated>cd /home/user/Projeto && npx vitest run tests/calculadoras/reclamatoria.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run tests/calculadoras/reclamatoria.test.ts: 0 failed
    - Caso 3 (justa causa): multaFGTS.inclui === false
    - Caso 4 (pedido demissao): multaFGTS.inclui === false E avisoPrevio.inclui === false
    - Caso 5/6: aviso previo maximo 90 dias respeitado
    - Projecoes: realista = totalBruto * 0.70 (verificado com valor fixo)
    - grep "PROJECOES\|decimal-utils" src/calculadoras/reclamatoria.ts retorna resultado
  </acceptance_criteria>
  <done>Todos os 9+ testes passam, 3 cenarios corretos, projecoes 100/70/55 verificadas, FGTS+40 confirmado</done>
</task>

</tasks>

<verification>
cd /home/user/Projeto && npx vitest run tests/calculadoras/reclamatoria.test.ts 2>&1
Esperado: X passed | 0 failed — com FGTS+40% = R$ 4.800 e projecoes verificadas

cd /home/user/Projeto && npx vitest run tests/calculadoras/ 2>&1
Esperado: Suite completa passa (todos os 5 arquivos de calculadoras)
</verification>

<success_criteria>
- npx vitest run tests/calculadoras/reclamatoria.test.ts: 100% passed
- Cenario justa causa: verbas corretas (saldo + ferias vencidas, sem FGTS multa)
- Cenario pedido demissao: sem multa FGTS, sem aviso previo indenizado
- Aviso previo proporcional Lei 12.506/2011: 30+3/ano, maximo 90 dias
- Projecoes otimista/realista/acordo com percentuais 100/70/55
- Honorarios 10% sobre totalBruto
- FGTS+40% sobre saldo total do FGTS (nao sobre salario)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-calculadoras-blindadas/01-06-SUMMARY.md` com:
- 3 cenarios implementados (sem_justa_causa, justa_causa, pedido_demissao)
- 3 projecoes com percentuais
- Verbas por cenario documentadas
- Resultados dos testes (N passed)

Tambem executar o suite completo e registrar total de testes:
cd /home/user/Projeto && npx vitest run tests/calculadoras/ 2>&1
</output>
