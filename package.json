{
  "name": "claude-code-juridico-testes",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "decimal.js": "^10.4.3"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.0",
    "bats": "^1.13.0",
    "vitest": "^3.0.0"
  }
}
